Drop UPSC PDF files directly into this folder.

Every `.pdf` file here is picked up automatically by the UPSC DOC section on
the Study Materials page —
no code changes needed. Add or remove a file and it appears/disappears
the next time the app is rebuilt (or on refresh during `npm run dev`).


I created a new branch for testing.
